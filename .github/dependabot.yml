version: 2
updates:
  # Keep the SHA-pinned GitHub Actions up to date (Dependabot bumps the pins
  # and the version-tag comments together).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
